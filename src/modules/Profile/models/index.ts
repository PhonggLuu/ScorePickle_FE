export interface UpdateProfile {
  userId: number;
  firstName: string;
  lastName: string;
  secondName: string;
  dateOfBirth: Date;
  gender: string;
  phoneNumber: string;
  avatarUrl: string;
  status: boolean;
}
